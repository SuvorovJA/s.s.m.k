spring.application.name: api-test
spring:
  cloud:
    kubernetes:
      discovery:
        ipAddress: ${cluster.ip}
        register: true
      client:
        masterUrl: ${cluster.ip}:8443
#        namespace: false
        trustCerts: true

#server.port: 0